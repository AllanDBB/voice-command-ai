{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Flutter Run Android",
			"type": "shell",
			"command": "flutter run -d emulator-5554 --debug --no-enable-impeller",
			"options": {
				"cwd": "${workspaceFolder}/app"
			},
			"isBackground": true,
			"problemMatcher": []
		}
	]
}